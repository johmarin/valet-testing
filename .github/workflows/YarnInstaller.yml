name: YarnInstallerTesting
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
env:
  SYSTEM.DEBUG: 'false'
jobs:
  serverjob:
    runs-on: windows-latest
    steps:
    - name: YarnInstaller
      run: |-
        npm install yarn@1.22.10
        yarn version
